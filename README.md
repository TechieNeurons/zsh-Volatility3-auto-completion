# zsh-Volatility3-auto-completion
Little script for getting auto completion for Volatility 3 plugins name

# Install
Download an move the file **_volpy** inside a directory of $fpath (for example: `/usr/local/share/zsh/site-functions`)